# Normalise to LF in the repository; checkout uses the platform default.
* text=auto eol=lf

# Binary assets must never be line-ending converted.
*.png binary
*.jpg binary
*.jpeg binary
*.avif binary
*.webp binary
*.ico binary
